modules:
    - type: rpm-ostree
      install:
        - NetworkManager-tui
        - NetworkManager-openvpn
        - gnome-keyring-pam
    - type: systemd
      system:
        enabled:
          - cosmic-greeter
